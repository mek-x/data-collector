version: 1

collectors:
  mqtt:
    type: mqtt
    params:
      url: ssl://mqtt:8883
      user: user
      pass: pass
  mqtt-2:
    type: mqtt
    params:
      url: ssl://mqtt2:8883
  cpuTemp:
    type: file
    params:
      interval: 5
  cmd:
    type: shell
    params:
      interval: 5

sinks:
  plotter:
    type: iotplotter
    params:
      url: http://iotplotter.com
      apikey: %%IOT_API_KEY%% # we can expand env variables using the %%VAR%% pattern
      feed: "123456789"
  gotify:
    type: gotify
    params:
      url: https://gotify
      token: ABCDEFG
  windy:
    type: windy
    params:
      apikey: abcdefg
      id: 1
  stdout:
    type: stdout

data:
  outside:
    collector: mqtt
    path: /topic1
    parser: jsonpath
    vars:
      temp: "$.temp"
      humi: "$.humi"
    conv:
      temp: "*10"
  cnt:
    collector: mqtt-2
    path: /topic/2
    parser: jsonpath
    vars:
      cnt: "$"
  cpu:
    collector: cpuTemp
    path: /sys/class/thermal/thermal_zone0/temp
    parser: regex
    vars:
      temp: "[0-9]*"
    conv:
      temp: float(temp) / 1000
  free:
    collector: cmd
    path: "df -BM | grep -E '/$' | awk '{print $4}'"
    parser: regex
    vars:
      space: "[0-9]*"
    conv:
      space: float(space) / 1024

dispatchers:
- type: cron
  param: "*/30 * * * * *" # once per 30 seconds
  sinks:
  - name: stdout
    type: expr # output will be marshalled as json
    spec: |
      {
        "data": {
          "temp": [ {"value": outside?.temp ?? 0} ],
          "humi": [ {"value": outside?.humi ?? 0} ]
        }
      }
  - name: stdout
    type: template
    spec: |
      Cnt: {{ .cnt.cnt }}
- type: cron
  param: "*/3 * * * * *"
  sinks:
  - name: stdout
    type: template
    spec: |
      CPU Temp: {{ .cpu.temp }}
      Free space: {{ .free.space }}GB
      Current shell: %%SHELL%%
